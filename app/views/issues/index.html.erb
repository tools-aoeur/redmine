<!-- to support view override using hooks -->
<% rendered_hooks_content = call_hook(:view_issues_index_view_override) %>

<% if rendered_hooks_content.present? %>
  <%= rendered_hooks_content %>
<% else %>
  <%= render partial: 'issues/index' %>
<% end %>
